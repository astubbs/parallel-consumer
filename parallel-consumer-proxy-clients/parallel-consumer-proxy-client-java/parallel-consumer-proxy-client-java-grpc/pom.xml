<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright (C) 2026 Antony Stubbs and contributors

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>parallel-consumer-proxy-client-java</artifactId>
        <groupId>bz.stub.parallelconsumer</groupId>
        <version>0.6.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <name>Parallel Consumer - Proxy Client Java gRPC</name>
    <artifactId>parallel-consumer-proxy-client-java-grpc</artifactId>

    <!--
        SKELETON, deliberately. The gRPC transport implements the shared API surface over the RPC
        hop to the sidecar, depending on the api module and the protocol module; it lands with the
        Java reference work on the language-proxy plan (astubbs#242,
        docs/plans/2026-08-14-001-feat-language-proxy-plan.md). Seeded empty so the module tree,
        aggregator entries and data fragments exist before any code does.
    -->

</project>
