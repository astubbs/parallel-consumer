// Copyright (C) 2026 Antony Stubbs and contributors
//
// Build scaffolding for the Go proxy client (astubbs#242). No dependencies, deliberately: the
// module exists to prove the Go toolchain reaches a running program, and a dependency would put
// the network between toolchain presence and that answer.

module github.com/astubbs/parallel-consumer/parallel-consumer-proxy-clients/parallel-consumer-proxy-client-go

go 1.23
