# Copyright (C) 2026 Antony Stubbs and contributors
#
# Skeleton, seeded by the language-proxy plan (astubbs#242). The Python wave owns this file and
# everything else in this directory; the pom.xml beside it wraps this project into the Maven
# reactor under -Dpc.foreignClients.

[project]
name = "parallel-consumer-proxy-client"
version = "0.0.0"
description = "Parallel Consumer proxy client for Python - skeleton, not yet implemented"
requires-python = ">=3.10"
