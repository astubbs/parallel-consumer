# Copyright (C) 2026 Antony Stubbs and contributors

"""Generated protobuf and gRPC stubs. Private: nothing outside this package may import them.

The modules beside this one are produced by ``tools/generate_proto.py`` (``make proto``) from the
frozen ``parallel-consumer-proxy-protocol/src/main/proto/parallelconsumer/proxy/v1/proxy.proto``
and are **committed deliberately**, so installing this package needs neither ``protoc`` nor the
schema file. ``make proto-check`` regenerates and fails on any diff, which is what stops the
committed copy drifting from the contract.

Do not edit them, and do not import them from user code: protobuf types are this library's wire
detail, and the surface that mirrors every other language's client is the package above.
"""
