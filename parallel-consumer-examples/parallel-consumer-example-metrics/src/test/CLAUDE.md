# parallel-consumer-examples/parallel-consumer-example-metrics/src/test/CLAUDE.md

Bridge only. Claude Code lazy-loads a nested `CLAUDE.md` when it reads or writes a file in that
directory, so the rules that bind while a test is being written arrive at the moment they are needed
rather than waiting to be looked up. Detail lives in `docs/testing.md`, which owns the topic.

@../../../../docs/testing-at-write-time.md
