<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright (C) 2020-2025 Confluent, Inc.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>bz.stub.parallelconsumer</groupId>
        <artifactId>parallel-consumer-examples</artifactId>
        <version>0.6.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>parallel-consumer-example-core</artifactId>
    <name>Kafka Parallel Consumer Example - Core</name>

    <dependencies>
        <!-- tag::exampleDep[] -->
        <dependency>
            <groupId>bz.stub.parallelconsumer</groupId>
            <artifactId>parallel-consumer-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- end::exampleDep[] -->
        <dependency>
            <groupId>bz.stub.parallelconsumer</groupId>
            <artifactId>parallel-consumer-core</artifactId>
            <version>${project.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>
        <!-- ConcurrencyObserver / RunSummary / SimulatedService - the instrumentation the industry
             examples wrap around their user function. Deliberately NOT published to Maven Central, which
             is exactly why every tagged (copied) region keeps it at arm's length: a reader's copy of a
             snippet must not reference a coordinate they cannot depend on. -->
        <dependency>
            <groupId>bz.stub.parallelconsumer</groupId>
            <artifactId>parallel-consumer-example-support</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- ExampleMockConsumers - the mock consumer every example test drives its app with, built once
             there instead of copy-pasted into each module's test. -->
        <dependency>
            <groupId>bz.stub.parallelconsumer</groupId>
            <artifactId>parallel-consumer-example-support</artifactId>
            <version>${project.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
    </dependencies>

    <repositories>
        <!-- tag::exampleRepo[] -->
        <repository>
            <name>Confluent</name>
            <id>confluent</id>
            <url>https://packages.confluent.io/maven/</url>
        </repository>
        <!-- end::exampleRepo[] -->
    </repositories>
</project>
