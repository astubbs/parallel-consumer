<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright (C) 2020-2026 Confluent, Inc. and contributors

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>bz.stub.parallelconsumer</groupId>
    <artifactId>parallel-consumer-parent</artifactId>
    <name>Kafka Parallel Consumer</name>
    <version>0.6.0.0-SNAPSHOT</version>
    <description>Parallel Apache Kafka client wrapper with client side queueing, a simpler consumer/producer API with key concurrency and extendable non-blocking IO processing.
    </description>
    <url>https://github.com/astubbs/parallel-consumer</url>
    <inceptionYear>2020</inceptionYear>

    <organization>
        <name>Antony Stubbs</name>
        <url>https://stub.bz</url>
    </organization>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
            <comments>A business-friendly OSS license</comments>
        </license>
    </licenses>

    <packaging>pom</packaging>

    <modules>
        <module>parallel-consumer-core</module>
        <module>parallel-consumer-vertx</module>
        <module>parallel-consumer-reactor</module>
        <module>parallel-consumer-mutiny</module>
        <module>parallel-consumer-examples</module>
    </modules>

    <developers>
        <developer>
            <id>astubbs</id>
            <name>Antony Stubbs</name>
            <email>antony.stubbs@gmail.com</email>
            <url>https://stub.bz</url>
            <organization>Antony Stubbs</organization>
            <organizationUrl>https://stub.bz</organizationUrl>
            <timezone>Europe/London</timezone>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com:astubbs/parallel-consumer.git</connection>
        <developerConnection>scm:git:git@github.com:astubbs/parallel-consumer.git</developerConnection>
        <url>https://github.com/astubbs/parallel-consumer.git</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <source.version>17</source.version>
        <release.target>8</release.target>

        <!-- when running mvn locally from cmd line, use default jvm for jvm8 - by default uses the runtime JVM -->
        <jvm.location>${java.home}</jvm.location>
        <jvm8.location>-insert jvm8 location via environment variable-</jvm8.location>
        <jvm9.location>-insert jvm9 location via environment variable-</jvm9.location>
        <!-- The mycila license plugin is SKIPPED BY DEFAULT for this fork: it knows only one header
             template (the Confluent one), so its format goal used to rewrite fork-original files with
             the wrong attribution, and its git-based year resolver auto-bumps years and breaks in git
             worktrees. Header conformance is enforced by bin/check-copyright-headers.sh instead
             (provenance-aware: upstream-derived files keep the Confluent header, fork-original files
             use the fork header - see AGENTS.md "Copyright rules for this fork"). That scanner runs
             in the `validate` phase via exec-maven-plugin (below), so a local `mvn` build catches
             header violations too, not only the copyright.yml GitHub Action. Re-enable the mycila
             plugin deliberately with -Dlicense.skip=false if you ever need its format goal on upstream files. -->
        <license.skip>true</license.skip>
        <!-- Skip the in-build copyright-header scanner (exec-maven-plugin, validate phase) with
             -Dcopyright.skip=true. Runs by default; unlike the mycila plugin it is worktree-safe. -->
        <copyright.skip>false</copyright.skip>
        <!-- default to format when on developer machines, check when in CI -->
        <license.mode>format</license.mode>
        <delombok.output>${project.basedir}/target/delombok</delombok.output>

        <!-- standard props -->
        <maven.version>3.6.3</maven.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <skipTests>false</skipTests>
        <skipITs>${skipTests}</skipITs>
        <skipUTs>${skipTests}</skipUTs>

        <!-- JUnit 5 test groups (failsafe). Override on the command line:
             -Dgroups=performance         (run only performance tests)
             -Dexcluded.groups=           (run everything including performance and quarantined)
             -Dexcluded.groups=transactions,performance  (skip multiple groups)
             `quarantined` = known-failing-on-master tests (see @Quarantined): out of the gating
             suites so green means mergeable, but still run by the non-gating CI lane
             (bin/quarantined-test.sh). -->
        <included.groups></included.groups>
        <excluded.groups>performance,quarantined</excluded.groups>

        <!-- version numbers -->
        <!-- plugins -->
        <mycila.version>5.1.1</mycila.version>
        <lombok.version>1.18.46</lombok.version>
        <auto-service.version>1.1.1</auto-service.version>
        <surefire.version>3.5.6</surefire.version>
        <spotbugs.version>4.10.3</spotbugs.version>
        <spotbugs-maven-plugin.version>4.10.3.0</spotbugs-maven-plugin.version>
        <pitest.version>1.25.8</pitest.version>
        <pitest-junit5.version>1.2.3</pitest-junit5.version>

        <!-- core -->
        <slf4j.version>2.0.18</slf4j.version>
        <kafka.version>3.9.1</kafka.version>
        <version.unij>0.1.3</version.unij>

        <!-- tests -->
        <parallel-tests>true</parallel-tests>
        <!-- Unit-test (surefire) JVM forks = process-level parallelism. Default 1 (no forking); the `ci`
             profile raises it to 1C (one fork per CPU core) so CI runs the unit suite in parallel across
             JVMs. This is reliable because each fork is a separate process (no shared static state between
             them) and single-threaded, unlike JUnit thread parallelism (`parallel-tests`) which contends on
             static state. Forks are reused to amortise JVM startup. -->
        <surefire.forkCount>1</surefire.forkCount>

        <!-- tests deps-->
        <junit.version>5.14.4</junit.version>
        <junit.platform.version>1.14.4</junit.platform.version>
        <testcontainers.version>1.21.4</testcontainers.version>
        <truth.version>1.4.5</truth.version>
        <flogger.version>0.9</flogger.version>
        <mockito.version>5.23.0</mockito.version>
        <!-- Aligns byte-buddy with what mockito 5.23 requires (it ships 1.17.7). The ancient
             wiremock-jre8 2.35.2 otherwise drags in byte-buddy 1.12.18, which wins the conflict
             and lacks the JAVA_V21 field mockito 5.23 needs -> MockitoInitializationException.
             Remove this pin once wiremock 3.x (a deferred major) drops the stale transitive.
             Test/provided scope only - not in the shipped artifact. See docs/inflight.md. -->
        <byte-buddy.version>1.17.7</byte-buddy.version>
        <truth-generator-maven-plugin.version>0.1.1</truth-generator-maven-plugin.version>
        <jabel.version>1.0.0</jabel.version>
        <logback.version>1.6.1</logback.version>
        <!-- Held at 1.13.0: keep the micrometer family version-aligned with
             micrometer-registry-prometheus (pinned to 1.12.x in example-metrics until the
             prometheus package rename is migrated). See docs/inflight.md. -->
        <micrometer-core.version>1.13.0</micrometer-core.version>
    </properties>

    <profiles>
        <profile>
            <!-- override source and target when working from IDE -->
            <id>intellij-idea-only</id>
            <activation>
                <property>
                    <name>idea.maven.embedder.version</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <!-- override source and target when working from IDE -->
                            <source>${source.version}</source>
                            <target>${source.version}</target>
                            <release>${source.version}</release>
                            <!-- disable Jabel -->
                            <annotationProcessors>
                                <annotationProcessor>lombok.launch.AnnotationProcessorHider$AnnotationProcessor
                                </annotationProcessor>
                            </annotationProcessors>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!-- For releasing, use jvm8 for testing -->
            <!-- currently broken -->
            <id>jvm8-release</id>
            <properties>
                <jvm.location>${jvm8.location}</jvm.location>
            </properties>
        </profile>
        <profile>
            <!-- For releasing, use jvm9 for testing -->
            <id>jvm9-release</id>
            <properties>
                <jvm.location>${jvm9.location}</jvm.location>
            </properties>
        </profile>
        <profile>
            <id>license-format</id>
            <activation>
                <os>
                    <!-- this doesnt' work? remove -->
                    <!-- improve to instead be !jenkins && !travis  / !CI -->
                    <family>mac</family>
                </os>
            </activation>
            <properties>
                <license.mode>format</license.mode>
            </properties>
        </profile>
        <profile>
            <id>github</id>
            <pluginRepositories>
                <pluginRepository>
                    <id>github-asciidoc-template-maven-plugin</id>
                    <url>https://maven.pkg.github.com/whelk-io/asciidoc-template-maven-plugin</url>
                </pluginRepository>
            </pluginRepositories>
        </profile>
        <profile>
            <!-- Profile to always run in Continuous Integration (Jenkins, Github Workflows etc) environments -->
            <id>ci</id>
            <properties>
                <license.mode>check</license.mode>
                <parallel-tests>false</parallel-tests>
                <!-- Parallelise the unit suite by FORKING JVMs (one per core) rather than JUnit threads:
                     ~3x faster on a multi-core box and reliable (no cross-thread static-state contention, so
                     it does not resurrect the thread-parallel flakes). On GitHub's 2-core runners 1C = 2 forks
                     (well-matched, no CPU starvation); on the self-hosted box it scales up to the core count. -->
                <surefire.forkCount>1C</surefire.forkCount>
                <!-- Fork-tail packing: run the SLOWEST test classes first so no fork idles at the tail.
                     Surefire's default runOrder=filesystem happened to schedule core's slowest class
                     (RunLengthEncoderTest, ~2min on CI) last, leaving one fork idle ~60s while the other
                     finished it. runOrder=balanced sorts classes by recorded runtime (slowest first) using
                     per-module statistics files; the checksum override (surefire 3.5.5+) pins the stats
                     file name to <module>/.surefire-pc-unit-times so the files can be CHECKED IN and work
                     on a fresh CI checkout. Do not delete the .surefire-pc-unit-times files - without them
                     balanced degrades to the unordered scan. Surefire refreshes them in place after each
                     -Pci run; only relative times matter, so occasional refreshes are enough. -->
                <surefire.runOrder>balanced</surefire.runOrder>
                <surefire.runOrder.statisticsFile.checksum>pc-unit-times</surefire.runOrder.statisticsFile.checksum>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-enforcer-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>enforce-versions</id>
                                <goals>
                                    <goal>enforce</goal>
                                </goals>
                                <phase>validate</phase>
                                <configuration>
                                    <rules>
                                        <!-- only enforce requireReleaseDeps when running in CI, not locally -->
                                        <requireReleaseDeps>
                                            <onlyWhenRelease>false</onlyWhenRelease>
                                            <failWhenParentIsSnapshot>false</failWhenParentIsSnapshot>
                                            <excludes>
                                                <exclude>bz.stub.parallelconsumer:</exclude>
                                            </excludes>
                                        </requireReleaseDeps>
                                    </rules>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>maven-central</id>
            <activation>
                <property>
                    <name>performRelease</name>
                    <value>true</value>
                </property>
            </activation>
            
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.8</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <!-- loopback mode required for headless CI (no TTY for passphrase prompt) -->
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.11.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                            <waitUntil>published</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencies>
        <!-- Build dependency -->
        <dependency>
            <groupId>com.github.bsideup.jabel</groupId>
            <artifactId>jabel-javac-plugin</artifactId>
            <version>${jabel.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Main -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>pl.tlinkowski.unij</groupId>
            <artifactId>pl.tlinkowski.unij.api</artifactId>
            <version>${version.unij}</version>
        </dependency>
        <dependency>
            <groupId>pl.tlinkowski.unij</groupId>
            <artifactId>pl.tlinkowski.unij.bundle.jdk8</artifactId>
            <version>${version.unij}</version>
            <scope>compile</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <version>${junit.platform.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <!-- Junit Pioneer pulls in an older version - force the matched version -->
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-commons</artifactId>
            <version>${junit.platform.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.7</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.google.auto.service</groupId>
            <artifactId>auto-service-annotations</artifactId>
            <version>${auto-service.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.google.truth</groupId>
            <artifactId>truth</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.google.truth.extensions</groupId>
            <artifactId>truth-java8-extension</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.google.flogger</groupId>
            <artifactId>flogger</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.google.flogger</groupId>
            <artifactId>flogger-slf4j-backend</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>one.util</groupId>
            <artifactId>streamex</artifactId>
            <version>0.9.0</version>
            <scope>test</scope>
        </dependency>
        <!-- Architecture rules (e.g. keep integration tests out of the unit suite) - all modules -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.3.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <!-- Pin byte-buddy so mockito 5.23 gets the version it needs (see byte-buddy.version
                 property); overrides the stale 1.12.18 pulled transitively by wiremock-jre8. -->
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy</artifactId>
                <version>${byte-buddy.version}</version>
            </dependency>
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy-agent</artifactId>
                <version>${byte-buddy.version}</version>
            </dependency>
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-core</artifactId>
                <version>${micrometer-core.version}</version>
            </dependency>
            <!-- External -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>kafka</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>postgresql</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.awaitility</groupId>
                <artifactId>awaitility</artifactId>
                <version>4.3.0</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>3.20.0</version>
            </dependency>
            <dependency>
                <groupId>com.google.truth</groupId>
                <artifactId>truth</artifactId>
                <version>${truth.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.google.truth.extensions</groupId>
                <artifactId>truth-java8-extension</artifactId>
                <version>${truth.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>io.stubbs.truth</groupId>
                <artifactId>truth-generator-api</artifactId>
                <version>${truth-generator-maven-plugin.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>33.6.0-jre</version>
            </dependency>
            <dependency>
                <groupId>me.tongfei</groupId>
                <artifactId>progressbar</artifactId>
                <version>0.10.2</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.google.flogger</groupId>
                <artifactId>flogger</artifactId>
                <version>${flogger.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.flogger</groupId>
                <artifactId>flogger-slf4j-backend</artifactId>
                <version>${flogger.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <scope>test</scope>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-engine</artifactId>
                <scope>test</scope>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-params</artifactId>
                <scope>test</scope>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit-pioneer</groupId>
                <artifactId>junit-pioneer</artifactId>
                <scope>test</scope>
                <version>2.3.0</version>
            </dependency>
            <dependency>
                <groupId>com.github.tomakehurst</groupId>
                <artifactId>wiremock-jre8</artifactId>
                <version>2.35.2</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-core</artifactId>
                <version>${logback.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-help-plugin</artifactId>
                <version>3.5.2</version>
                <executions>
                    <execution>
                        <id>show-profiles</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>active-profiles</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.sonatype.ossindex.maven</groupId>
                <artifactId>ossindex-maven-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <scope>runtime</scope>
                    <fail>false</fail>
                </configuration>
                <executions>
                    <execution>
                        <id>audit-dependencies</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>audit</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.soebes.maven.plugins</groupId>
                <artifactId>echo-maven-plugin</artifactId>
                <version>0.5.0</version>
                <!-- very useful for debugging maven runtime state -->
                <!--                <configuration>-->
                <!--                    <echos>-->
                <!--                        <echo>vars: ${JAVA_HOME} ${java.home} ${jvm.location}</echo>-->
                <!--                    </echos>-->
                <!--                </configuration>-->
            </plugin>
            <plugin>
                <groupId>io.whelk.asciidoc</groupId>
                <artifactId>asciidoc-template-maven-plugin</artifactId>
                <version>1.0.21</version>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>process</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <templateDirectory>src/docs/</templateDirectory>
                    <templateFile>README_TEMPLATE.adoc</templateFile>
                    <outputDirectory>./</outputDirectory>
                    <outputFile>README.adoc</outputFile>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <autoVersionSubmodules>true</autoVersionSubmodules>
                    <tagNameFormat>v@{project.version}</tagNameFormat>
                    <!-- Tag-as-truth release (see .github/workflows/release.yml, dispatch-triggered):
                         release:prepare rewrites the poms, makes the two release commits
                         ("prepare release vX" + "prepare for next development iteration"), tags vX,
                         and PUSHES them to master (pushChanges=true) using a repo-admin PAT that
                         bypasses the master ruleset's pull_request requirement. release.yml then checks
                         out the exact tag vX and deploys it. The tag is the single source of truth -
                         nothing scans history.

                         preparationGoals=clean install (not the "poms only" validate we first tried):
                         the modules cross-depend on parallel-consumer-core's test-jar
                         (bz.stub.parallelconsumer:parallel-consumer-core:jar:tests), which does not exist
                         until core is packaged+installed. A validate-only prepare therefore fails to
                         resolve it (tries Central, 404). "clean install" builds+installs the reactor so
                         the test-jars are present, cross-module deps resolve, and the release-versioned
                         poms are proven to compile before the tag is cut. -DskipTests (passed via
                         release.yml's -Darguments) keeps prepare fast - the "refuse red master" guard
                         already gates on a green test run. -->
                    <preparationGoals>clean install</preparationGoals>
                    <pushChanges>true</pushChanges>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.mycila</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>${mycila.version}</version>
                <inherited>false</inherited>
                <!-- To apply the license run: -->
                <!-- ↪ mvn com.mycila:license-maven-plugin:format-->
                <!-- To skip: -Dlicense.skip -->
                <configuration>
                    <aggregate>true</aggregate>
                    <licenseSets>
                        <licenseSet>
                            <inlineHeader>Copyright (C) 2020-${license.git.copyrightYears} Confluent, Inc. and contributors
                            </inlineHeader>
                            <excludes>
                                <exclude>**/release-pom.xml</exclude>
                                <exclude>**/target/**/*</exclude>
                                <exclude>**/README*</exclude>
                                <!-- don't think this is needed, as it's excluded by default. But running really slow on OSx so thought should check -->
                                <!-- https://github.com/mathieucarbou/license-maven-plugin/issues/298-->
                                <!-- Ideally the fix would be this issue: https://github.com/mathieucarbou/license-maven-plugin/issues/126  Ignore items from .gitignore #126-->
                                <exclude>.ci/**/*</exclude>
                                <exclude>.git/**/*</exclude>
                                <!-- exclude semaphore yml files -->
                                <exclude>.semaphore/*</exclude>
                                <!-- exclude semaphore build time files -->
                                <exclude>bin/</exclude>
                                <exclude>.m2/</exclude>
                                <exclude>mk-include/</exclude>
                                <!-- Very odd artifact on Jenkins -->
                                <!--00:01:15.201  [INFO] Checking licenses...-->
                                <!--00:01:17.095  [WARNING] Unknown file extension: ********-->
                                <!--00:01:17.096  [WARNING] Missing header in: ********-->
                                <exclude>********</exclude>
                            </excludes>
                        </licenseSet>
                    </licenseSets>
                    <mapping>
                        <java>JAVAPKG_STYLE</java>
                    </mapping>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>com.mycila</groupId>
                        <artifactId>license-maven-plugin-git</artifactId>
                        <version>${mycila.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <goals>
                            <goal>${license.mode}</goal>
                        </goals>
                        <phase>process-sources</phase>
                    </execution>
                </executions>
            </plugin>
            <!-- Provenance-aware copyright-header conformance IN THE BUILD (fork policy, AGENTS.md).
                 Runs bin/check-copyright-headers.sh in `validate` so `mvn` catches header violations
                 locally, not only via the copyright.yml GitHub Action. Once at the root (inherited=false).
                 The script cd's to the git top-level itself, so ${project.basedir} (the root) is fine.
                 Skip with -Dcopyright.skip=true. Needs the fork-point commit in history; on a shallow
                 clone the scanner warns and skips (CI's copyright.yml uses fetch-depth: 0 + strict mode). -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>check-copyright-headers</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <skip>${copyright.skip}</skip>
                            <executable>bash</executable>
                            <arguments>
                                <argument>${project.basedir}/bin/check-copyright-headers.sh</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>com.google.auto.service</groupId>
                            <artifactId>auto-service</artifactId>
                            <version>${auto-service.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <!-- jabel setup-->
                        <path>
                            <groupId>com.github.bsideup.jabel</groupId>
                            <artifactId>jabel-javac-plugin</artifactId>
                            <version>${jabel.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <!-- enable language preview features -->
                    <source>${source.version}</source>
                    <target>${release.target}</target>
                    <release>${release.target}</release>
                    <annotationProcessors>
                        <annotationProcessor>lombok.launch.AnnotationProcessorHider$AnnotationProcessor
                        </annotationProcessor>
                    </annotationProcessors>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
                <configuration>
                    <trimStackTrace>false</trimStackTrace>
                    <forkCount>${surefire.forkCount}</forkCount>
                    <reuseForks>true</reuseForks>
                    <excludes>
                        <!-- <exclude>**/*integration*/**/*.java</exclude> too aggressive? -->
                        <exclude>**/integrationTest*/**/*.java</exclude>
                        <exclude>**/*IT.java</exclude>
                    </excludes>
                    <jvm>${jvm.location}/bin/java</jvm>
                    <skipTests>${skipUTs}</skipTests>
                    <!-- Same group filtering as failsafe below - without this binding, surefire ignores
                         the quarantine/performance tags entirely and a @Quarantined UNIT test would
                         still run (and fail) in the gating Unit Tests lane (ce-review P1 finding). -->
                    <groups>${included.groups}</groups>
                    <excludedGroups>${excluded.groups}</excludedGroups>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${surefire.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <includes>
                        <!-- using filename matching -->
                        <!-- Integration tests need to be in a package named `integrationTest` -->
                        <include>**/integrationTest*/**/*.java</include>
                    </includes>
                    <jvm>${jvm.location}/bin/java</jvm>
                    <skipTests>${skipTests}</skipTests>
                    <skipITs>${skipITs}</skipITs>
                    <parallel>methods</parallel>
                    <!-- Performance tests are excluded by default and run on dedicated hardware via the
                         performance.yml workflow on a self-hosted runner. Override with -Dgroups=performance
                         to run only performance tests, or -Dexcluded.groups= to run everything. -->
                    <groups>${included.groups}</groups>
                    <excludedGroups>${excluded.groups}</excludedGroups>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>prepare-agent-integration</id>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report-integration</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>report-integration</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs-maven-plugin.version}</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Medium</threshold>
                    <xmlOutput>true</xmlOutput>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>com.github.spotbugs</groupId>
                        <artifactId>spotbugs</artifactId>
                        <version>${spotbugs.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>${pitest.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-junit5-plugin</artifactId>
                        <version>${pitest-junit5.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.6.3</version>
                <executions>
                    <execution>
                        <id>enforce-versions</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <phase>validate</phase>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[${source.version},)</version>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>3.6.3</version>
                                </requireMavenVersion>
                                <requireMavenVersion>
                                    <version>3.6.3</version>
                                </requireMavenVersion>
                                <bannedDependencies>
                                    <searchTransitive>true</searchTransitive>
                                    <excludes>
                                        <exclude>log4j:*:*:*:runtime</exclude>
                                        <exclude>log4j:*:*:*:compile</exclude>
                                        <exclude>log4j:*:*:*:test</exclude>
                                        <exclude>org.apache.logging.log4j:log4j-core:*:*:runtime</exclude>
                                        <exclude>org.apache.logging.log4j:log4j-core:*:*:compile</exclude>
                                        <!-- Javafaker is abandoned and has CVEs and bugs -->
                                        <exclude>com.github.javafaker</exclude>
                                    </excludes>
                                </bannedDependencies>
                                <reactorModuleConvergence />
                                <banDuplicatePomDependencyVersions />
                                <requireSameVersions />
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- https://stackoverflow.com/a/1733745/105741-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                        <configuration>
                            <!-- Don't produce a test-jar when there are no test classes
                                 (e.g. the parent pom module or examples). Otherwise an empty
                                 test-jar gets generated, signed, and uploaded to Maven Central. -->
                            <skipIfEmpty>true</skipIfEmpty>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <!-- integration test sources -->
                    <execution>
                        <id>add-integration-test-source</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>add-test-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>src/test-integration/java</source>
                                <source>${project.build.directory}/generated-sources</source>
                            </sources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>add-integration-test-resource</id>
                        <phase>generate-test-resources</phase>
                        <goals>
                            <goal>add-test-resource</goal>
                        </goals>
                        <configuration>
                            <resources>
                                <resource>
                                    <directory>src/test-integration/resources</directory>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
                <version>4.0.0-M15</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
                <version>3.9.0</version>
            </plugin>
            <plugin>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok-maven-plugin</artifactId>
                <version>1.18.20.0</version>
                <configuration>
                    <sourceDirectory>${project.basedir}/src/main/java</sourceDirectory>
                    <outputDirectory>${delombok.output}</outputDirectory>
                    <addOutputDirectory>false</addOutputDirectory>
                </configuration>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>delombok</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.12.0</version>
                <configuration>
                    <sourcepath>${delombok.output}</sourcepath>
                    <sourcepath>${delombok.output}</sourcepath>
                    <!--
                    Force javadoc to ignore things like "unknown tag: implNote"
                    and "no @param for <C>"
                    -->
                    <additionalOptions>-Xdoclint:none</additionalOptions>
                    <javadocExecutable>${java.home}/bin/javadoc</javadocExecutable>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <source>${source.version}</source>
                            <release>${source.version}</release>
                            <!-- Source goals - change to true? -->
                            <failOnWarnings>false</failOnWarnings>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.4.0</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.11.0</version>
                <executions>
                    <execution>
                        <phase>initialize</phase>
                        <goals>
                            <goal>tree</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!-- for use in idea -->
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
                <version>2.21.0</version>
                <configuration>
                    <!-- Risk-averse upgrade policy: ignore pre-releases (alpha/beta/-Mn/RC) and
                         Confluent -ce/-ccs kafka builds. Combine with -DallowMajorUpdates=false to
                         cap to newest non-major. Applies to display-*-updates / update-* goals. -->
                    <!-- multiModuleProjectDirectory = reactor root (bin/ only exists there);
                         ${project.basedir} would be wrong for child modules. -->
                    <rulesUri>file://${maven.multiModuleProjectDirectory}/bin/deps-version-rules.xml</rulesUri>
                </configuration>
            </plugin>
        </plugins>

        <!-- Management -->
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>4.0.0-M15</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-project-info-reports-plugin</artifactId>
                    <version>3.9.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>3.1.4</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>3.1.4</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>3.11.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <!--
      Repository order matters: Maven queries sequentially and does not fall
      through automatically on a hang. Put the fastest/most-covering repo first
      so most artifacts resolve on the first hit; custom repos only consulted
      for artifacts that aren't on Central. See .mvn/maven.config for the
      per-connection timeouts that guard against any repo stalling.
    -->
    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo1.maven.org/maven2/</url>
        </repository>
        <repository>
            <id>confluent</id>
            <url>https://packages.confluent.io/maven/</url>
        </repository>
        <repository>
            <id>astubbs-truth-generator</id>
            <url>https://packagecloud.io/astubbs/truth-generator/maven2</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>
    <pluginRepositories>
        <pluginRepository>
            <id>central</id>
            <url>https://repo1.maven.org/maven2/</url>
        </pluginRepository>
        <pluginRepository>
            <id>astubbs-truth-generator</id>
            <url>https://packagecloud.io/astubbs/truth-generator/maven2</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>
