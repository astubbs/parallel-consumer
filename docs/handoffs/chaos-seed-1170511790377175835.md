---
artifact_contract: "ce-handoff/v1"
created_at: "2026-09-01T02:30:00Z"
title: "Chaos seed 1170511790377175835 - a clean-tree ChaosRevokeUnderWorkDrainIT kill"
summary: "An unrecorded confluentinc#857-family chaos failure captured on a docs-only PR whose tree is byte-identical to master in the deadlock file, so its stack frames compare by line number."
keywords: ["chaos-seed", "confluentinc-857", "astubbs-29", "ChaosRevokeUnderWorkDrainIT", "control-arm", "bug-857-family"]
cwd: "/home/astubbs/git/parallel-consumer"
resume_focus: "Decide whether to record this sighting in docs/inflight/bug-857-family.md on the branch that owns the investigation, and whether to replay the seed."
repository: "astubbs/parallel-consumer"
repo_root_sha: "713c7468d5ecda55a2190d38e698cda017e91259"
branch: "docs/180-build-recipe"
head: "e7b1dda8898a73a51428bfdc66472b20a7546640"
---

# Chaos seed `1170511790377175835` — an unrecorded kill on an uncontaminated tree

## Why this is being handed over

The failure was observed while babysitting **astubbs#200**, a documentation-only PR that has nothing
to do with the confluentinc#857 family. `docs/inflight/bug-857-family.md` belongs to a live
investigation on other branches (astubbs#29 and relatives), and writing a sighting into it from an
unrelated docs PR would have been wrong — so the capture was deliberately left unrecorded.

**Nothing has been written to the ledger. No branch was modified. This document is the only record.**

## What happened

`Chaos Pain Suite` failed once on astubbs#200, then passed on a plain re-run of the same job — same
tree, new seed. No test was loosened, retried, quarantined, or otherwise touched.

- **Test**: `ChaosRevokeUnderWorkDrainIT.revokeUnderDrainingStopsStaysProtocolHonest`
- **Seed**: `1170511790377175835`
- **Failed run** (attempt 1):
  https://github.com/astubbs/parallel-consumer/actions/runs/33458796933/job/99704435326
- **Passing re-run** (attempt 2, same head, different seed): job `99708030261` in the same run
- **Head**: `e7b1dda8898a73a51428bfdc66472b20a7546640` on `docs/180-build-recipe` (astubbs#200)
- **Captured symptom**: `POLL THREAD AT TIMEOUT: BLOCKED ... Lock: AtomicBoolean@5e65d395, held by:
  pc-control-PC-44`

Replay follows the ledger's own recorded form:

    ./mvnw -Pci -pl parallel-consumer-core -am verify -DskipUTs=true \
      -Dincluded.groups=chaos -Dexcluded.groups= -Dchaos.seed=1170511790377175835

## What makes this arm unusually clean — verified, not asserted

astubbs#200 changes **zero `.java` files**. Both checks were run against the branch:

- `git diff --name-only origin/master HEAD | grep '\.java$'` → **empty**
- `git diff --stat origin/master HEAD -- '*AbstractParallelEoSStreamProcessor.java'` → **empty**

So `parallel-consumer-core/src/main/java/bz/stub/parallelconsumer/internal/AbstractParallelEoSStreamProcessor.java`
is **byte-identical to master**, and the captured frames compare to master by line number without
translation — unlike the astubbs#205 captures the ledger already holds. Confirmed on that file at
this head:

- **line 1775** → `synchronized (commitCommand) {`
- **line 580** → `commitOffsetsThatAreReady();`

Those are the two sides of the pair `bug-857-family.md` names in its opening section: the poll thread
in `onPartitionsRevoked` against the control thread in `commitOffsetsThatAreReady`.

> Note: the original babysit report labelled line 580 as `onPartitionsRevoked`. That is the wrong
> label — 580 is the `commitOffsetsThatAreReady()` call, i.e. the control-thread side. The frames are
> right; only the labelling was off. Corrected here so it is not propagated.

## Where it sits against what the ledger already has

**Confirmed absent**: `grep -c '1170511790377175835'` against
`origin/master:docs/inflight/bug-857-family.md` → **0**. This seed is not recorded anywhere.

**One claim from the original report needs correcting before it is relied on.** It said this is "the
clean not-PR-introduced control that ledger says it lacks". That overstates it — the ledger's
**eleventh sighting (2026-08-19)** already records seed `6825864417772979246` with exactly such an
arm, including a `5c377ec04` (master) local control that went RED. So this is not the first.

What is genuinely different here:

- **Different scenario.** The eleventh sighting is `ChaosRevokeUnderWorkIT`; this is the **Drain**
  variant, `ChaosRevokeUnderWorkDrainIT`.
- **Different signature.** The eleventh is `CLASS2_STALL/LAG_STAGNATION` — a timing bound. This is a
  **BLOCKED poll thread with a named lock holder**, which is the deadlock signature itself rather
  than a proxy for it.
- **That distinction matters for an operator ruling already in the ledger.** `bug-857-family.md`
  carries a **READ BEFORE RESUMING astubbs#29** block dated 2026-08-25 establishing that
  `CLASS2_STALL` reds are a timing proxy, *not* sightings of this family, and instructing that such a
  red is not evidence either way. **This capture is not a `CLASS2_STALL`**, so that ruling does not
  dispose of it — which is precisely why it seemed worth keeping.
- **Uncontaminated tree**, as measured above.

## The experiment the ledger says is unrun

`bug-857-family.md` (around its tenth/eleventh sightings, anchor `cheapest next experiment the family
has`) frames the open question as whether the arm follows the **tree** or the **seed**.

Here it **followed the seed**: identical tree, one seed red, the next green. That is a single data
point from a re-run rather than a designed replay, and it has *not* been replayed on demand — so it
is weaker evidence than the eleventh sighting's, which reproduced twice off CI.

## Status and caveats

- **Not recorded anywhere.** No edit to `bug-857-family.md`, no edit to
  `docs/inflight/test-untracked-ci-flakes.md`, no branch touched.
- **Not independently reproduced.** The seed has not been replayed locally by anyone. Whether it
  replays RED is unknown and is the obvious first thing to establish.
- **The failure itself was not observed first-hand by the session writing this document** — it comes
  from a babysit agent's report. The run and its attempt-1 failure were then verified via the GitHub
  API, and the tree claims were verified directly against the branch. The `AtomicBoolean@5e65d395` /
  `pc-control-PC-44` detail is from the agent's report and has not been re-read from the job log.
- **astubbs#200 is unaffected either way.** It is docs-only, its chaos lane is green on re-run, and
  nothing about this capture blocks or changes it.

## Load-bearing references

- `docs/inflight/bug-857-family.md` — the ledger. Read the **READ BEFORE RESUMING astubbs#29** block
  (2026-08-25) first; then the eleventh sighting (anchor `the first seed in this family that replays
  RED on demand`) for the strongest existing arm and the table format sightings use; and the opening
  section (anchor `onPartitionsRevoked`) for the thread pair.
- `docs/solutions/best-practices/a-timing-bound-used-as-a-correctness-gate-manufactures-its-own-evidence.md`
  — why a ~154s `CLASS2_STALL` signature is not self-evidently a family occurrence. Relevant as
  contrast: this capture is *not* that shape.
- `parallel-consumer-core/src/main/java/bz/stub/parallelconsumer/internal/AbstractParallelEoSStreamProcessor.java`
  — lines 1775 and 580 as above, at master's content.

## Plausible next step

Replay the seed with the command above, first on master and then wherever the astubbs#29
investigation currently sits, and record the result in `bug-857-family.md` **on the branch that owns
that file** — as a new dated sighting, not by editing an existing one. If it replays green, that is
still worth recording: the ledger explicitly notes seeds that "replay clean", and a Drain-variant
green is a data point the family does not have.

Whether to do any of that is the user's call; nothing here has been acted on.
