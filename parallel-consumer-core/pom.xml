<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright (C) 2020-2025 Confluent, Inc.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>bz.stub.parallelconsumer</groupId>
        <artifactId>parallel-consumer-parent</artifactId>
        <version>0.6.0.0-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>

    <artifactId>parallel-consumer-core</artifactId>
    <name>Kafka Parallel Consumer Core</name>

    <dependencies>
        <!-- Main -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
            <version>${kafka.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.luben</groupId>
            <artifactId>zstd-jni</artifactId>
            <version>1.5.7-12</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.xerial.snappy</groupId>
            <artifactId>snappy-java</artifactId>
            <version>1.1.10.8</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit-pioneer</groupId>
            <artifactId>junit-pioneer</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>kafka</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>me.tongfei</groupId>
            <artifactId>progressbar</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.13</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.github.tomakehurst</groupId>
            <artifactId>wiremock-jre8</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.threeten</groupId>
            <artifactId>threeten-extra</artifactId>
            <version>1.10.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.stubbs.truth</groupId>
            <artifactId>truth-generator-api</artifactId>
            <version>${truth-generator-maven-plugin.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>uk.co.jemos.podam</groupId>
            <artifactId>podam</artifactId>
            <version>8.0.2.RELEASE</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- JUnit 5 thread-parallelism for THIS module's tests.

                 This used to live in src/test/resources/junit-platform.properties, which was a bug: that
                 file is packaged at the ROOT of the core tests jar, and eight modules depend on that jar
                 (the four integrations plus every example). JUnit reads the first junit-platform.properties
                 on the classpath, so all eight silently ran with whatever value ${parallel-tests} happened
                 to hold WHEN CORE WAS BUILT - their own setting was never consulted.

                 That inverted the intent below: the `ci` profile sets parallel-tests=false on purpose,
                 because JUnit thread parallelism contends on static state and CI parallelises by forking
                 JVMs instead. A downstream module could nonetheless inherit enabled=true, factor=20 from a
                 locally-built jar.

                 As surefire/failsafe configurationParameters the setting is per-module by construction -
                 each module evaluates its own ${parallel-tests}, nothing is packaged, nothing leaks, and
                 these outrank a properties file if one is ever reintroduced. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <properties>
                        <configurationParameters>
                            junit.jupiter.execution.parallel.enabled=${parallel-tests}
                            junit.jupiter.execution.parallel.mode.default=concurrent
                            <!-- high multiple: most of these tests are io bound, not cpu bound -->
                            junit.jupiter.execution.parallel.config.dynamic.factor=20
                        </configurationParameters>
                    </properties>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <properties>
                        <configurationParameters>
                            junit.jupiter.execution.parallel.enabled=${parallel-tests}
                            junit.jupiter.execution.parallel.mode.default=concurrent
                            junit.jupiter.execution.parallel.config.dynamic.factor=20
                        </configurationParameters>
                    </properties>
                </configuration>
            </plugin>

            <plugin>
                <groupId>io.stubbs.truth</groupId>
                <artifactId>truth-generator-maven-plugin</artifactId>
                <version>${truth-generator-maven-plugin.version}</version>
                <configuration>
                    <cleanTargetDir>false</cleanTargetDir>
                    <useHas>false</useHas>
                    <useGetterForLegacyClasses>true</useGetterForLegacyClasses>
                    <releaseTarget>8</releaseTarget>
                    <classes>
                        <class>io.confluent.parallelconsumer.PollContext</class>
                        <class>io.confluent.parallelconsumer.ParallelEoSStreamProcessor</class>
                        <class>io.confluent.parallelconsumer.internal.ProducerManager</class>
                        <class>io.confluent.parallelconsumer.state.WorkContainer</class>
                        <class>io.confluent.parallelconsumer.state.WorkManager</class>
                        <class>io.confluent.parallelconsumer.state.PartitionState</class>
                        <class>io.confluent.parallelconsumer.state.ProcessingShard</class>
                        <class>io.confluent.parallelconsumer.state.ShardKey</class>
                        <class>io.confluent.parallelconsumer.offsets.OffsetEncoding</class>
                    </classes>
                    <legacyClasses>
                        <!-- todo check legacy is recursive -->
                        <class>org.apache.kafka.clients.consumer.OffsetAndMetadata</class>
                        <class>org.apache.kafka.clients.consumer.ConsumerRecord</class>
                        <class>org.apache.kafka.clients.consumer.ConsumerRecords</class>
                        <class>org.apache.kafka.clients.consumer.Consumer</class>
                        <class>org.apache.kafka.clients.producer.RecordMetadata</class>
                        <class>org.apache.kafka.clients.producer.ProducerRecord</class>
                        <class>org.apache.kafka.clients.producer.Producer</class>
                    </legacyClasses>
                    <entryPointClassPackage>io.confluent.parallelconsumer</entryPointClassPackage>
                </configuration>
                <executions>
                    <execution>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


</project>
