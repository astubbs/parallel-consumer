<!--

    Copyright (C) 2020-2022 Confluent, Inc.
    Modifications Copyright (C) 2026 Antony Stubbs and contributors

-->
<configuration packagingData="true" scan="true" scanPeriod="5 seconds" debug="false">

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <!--            <pattern>%d{mm:ss.SSS} [%thread] %-5level (%class{0}.java:%line\)#%M - %msg%n</pattern>-->
            <!--            <pattern>[%thread] %-5level .\(%class{0}.java:%line\)#%M - %msg%n</pattern>-->
            <!--            <pattern>[%thread] %-5level .\(%file:%line\)#%M - %msg%n</pattern>-->
            <!--            <pattern>%highlight(%-5level) %d{yyyy-MM-dd'T'HH:mm:ss.SSS} %yellow([%thread]) %blue(%logger{36}\(%class{0}.java:%line\)) %msg%n</pattern>-->
            <!--            <pattern>%highlight(%-5level) %d{yyyy-MM-dd'T'HH:mm:ss.SSS} %yellow([%thread]) %blue(%logger{36}\(%file:%line\)) %msg%n</pattern>-->
            <!--            <pattern>%highlight(%-5level) %yellow([%thread]) %blue(\(%file:%line\)) %cyan(#%M) %msg%n</pattern>-->
            <pattern>%d{mm:ss.SSS} %yellow(%X{pcId}) %highlight(%-5level) %yellow([%thread]) %X{offset} %cyan(\(%file:%line\)#%M) %msg%n
            </pattern>
            <!--            <pattern>%highlight(%-5level) %yellow([%thread]) %cyan(\(%logger{36}:%line#%M\))  %msg%n</pattern>-->
        </encoder>
    </appender>

    <!-- DEFAULT QUIET, RAISED BY FLAG - do not edit this file to see more output.
         Both levels below read the `pc.log.level` system property, so the whole harness is turned up
         from the command line and there is nothing to remember to revert:

             ./mvnw test -Dpc.log.level=debug -Dtest=TheOneTest      # PC's own logging
             ./mvnw test -Dpc.log.level=trace -Dtest=TheOneTest      # everything PC emits

         Editing the file works too, and used to be the only way - which is exactly why the default
         drifted to `info` here while every sibling module sat at `warn`. A flag cannot be committed
         by accident; an edited default can, and did. docs/testing.md owns the how-to.

         Why `warn` and not `info`: PC logs a full ParallelConsumerOptions toString per construction at
         INFO. Measured over the 387-test core unit suite, that was 196 banner dumps inside 5,520 lines
         of output, now 3,687. Reading one failing class meant scrolling past its options dump once per
         test. -->

    <!-- ===================================================================================
         SILOED LOG STREAMS - copies, not diversions.

         Every logger below keeps its default additivity, so everything still reaches STDOUT
         exactly as before. These are ADDITIONAL copies routed by concern, because during
         analysis a concern is either the only thing you want or the only thing in the way.

         The rule for what earns a file: anything tangential to running the product. Detectors
         and probes exist to observe the test, not to be the product. The harness is not the
         product. Those two are the ones that get lost in a shared stream.

         Levels are NOT set here - they are inherited, so `pc.log.level` still governs and
         bin/check-test-log-config.sh still holds. Raising the harness raises these with it.

         Written under target/ so they are build output. Start with what analysis actually
         asked for this week and add streams as they earn it, rather than guessing the set.
         ================================================================================= -->

    <!-- PROVE which config loaded rather than assuming - logback prints the file URL it read:
             -Dlogback.statusListenerClass=ch.qos.logback.core.status.OnConsoleStatusListener
         The integration suite has its own profile, selected explicitly:
             -Dlogback.configurationFile=logback-integration-test.xml
         docs/logging.md owns the why. -->

    <!-- The local name deliberately differs from the system property it reads. Written as
         <property name="pc.log.dir" value="${pc.log.dir:-target/pc-logs}"/> the definition refers to
         itself, and logback resolves the self-reference to the default instead of to -Dpc.log.dir -
         so the override silently did nothing and every run wrote to target/pc-logs whatever was
         passed. Proved by putting -Dpc.log.dir on the fork command line and watching the files land
         in the default directory anyway. Keep the two names different. -->
    <property name="siloDir" value="${pc.log.dir:-target/pc-logs}"/>

    <!-- Two layouts, named once. Seven appenders used to carry a copy each, which is how a silo
         quietly ends up formatted differently from its siblings and stops being greppable with the
         same expression. The level/detector streams differ from the whole-run ones in two ways on
         purpose: a wider logger field, because a stack trace is worth attributing precisely, and
         %ex, because an error line without its exception is not an error report. -->
    <property name="SILO_PATTERN" value="%d{HH:mm:ss.SSS} %X{pcId} [%thread] %logger{24} - %msg%n"/>
    <property name="SILO_PATTERN_WITH_STACK"
              value="%d{HH:mm:ss.SSS} %X{pcId} [%thread] %logger{36} - %msg%n%ex"/>

    <!-- EVERY error, and EVERY warning, for the whole run - never rolled, never truncated.
         A ring buffer is the right answer to volume and the wrong answer to a warning from two
         days ago, which is the line you want when something fails on day three. -->
    <appender name="ERRORS" class="ch.qos.logback.core.FileAppender">
        <file>${siloDir}/errors.log</file>
        <append>true</append>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level><onMatch>ACCEPT</onMatch><onMismatch>DENY</onMismatch>
        </filter>
        <encoder><pattern>${SILO_PATTERN_WITH_STACK}</pattern></encoder>
    </appender>

    <appender name="WARNINGS" class="ch.qos.logback.core.FileAppender">
        <file>${siloDir}/warnings.log</file>
        <append>true</append>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>WARN</level><onMatch>ACCEPT</onMatch><onMismatch>DENY</onMismatch>
        </filter>
        <encoder><pattern>${SILO_PATTERN_WITH_STACK}</pattern></encoder>
    </appender>

    <appender name="PROBES" class="ch.qos.logback.core.FileAppender">
        <file>${siloDir}/probes.log</file>
        <append>true</append>
        <encoder><pattern>${SILO_PATTERN}</pattern></encoder>
    </appender>

    <appender name="HARNESS" class="ch.qos.logback.core.FileAppender">
        <file>${siloDir}/harness.log</file>
        <append>true</append>
        <encoder><pattern>${SILO_PATTERN}</pattern></encoder>
    </appender>

    <appender name="PC_COMMIT" class="ch.qos.logback.core.FileAppender">
        <file>${siloDir}/pc-commit-offsets.log</file>
        <append>true</append>
        <encoder><pattern>${SILO_PATTERN}</pattern></encoder>
    </appender>

    <appender name="PC_LIFECYCLE" class="ch.qos.logback.core.FileAppender">
        <file>${siloDir}/pc-poll-rebalance-lifecycle.log</file>
        <append>true</append>
        <encoder><pattern>${SILO_PATTERN}</pattern></encoder>
    </appender>

    <appender name="PC_STATE" class="ch.qos.logback.core.FileAppender">
        <file>${siloDir}/pc-shard-work-state.log</file>
        <append>true</append>
        <encoder><pattern>${SILO_PATTERN}</pattern></encoder>
    </appender>

    <!-- Routing. No level attributes on purpose: these inherit, so pc.log.level still governs. -->

    <!-- Detectors and probes - the instruments. "Did the detector fire" should be one small file,
         not a grep of a 38,000-line run log. Answering that took several wrong greps on 2026-08-28,
         and the finding that a detector stayed SILENT on a third of failures was noticed by
         accident rather than by looking. -->
    <logger name="bz.stub.parallelconsumer.integrationTests.chaostests.ProgressProbe"><appender-ref ref="PROBES"/></logger>
    <logger name="bz.stub.parallelconsumer.integrationTests.chaostests.KeyOrderLedger"><appender-ref ref="PROBES"/></logger>
    <logger name="bz.stub.parallelconsumer.integrationTests.AmbientProbeExtension"><appender-ref ref="PROBES"/></logger>
    <logger name="bz.stub.parallelconsumer.internal.PollThreadStallDiagnosis"><appender-ref ref="PROBES"/></logger>

    <!-- The harness: what the test did TO the system, separate from what the system did. -->
    <logger name="bz.stub.parallelconsumer.integrationTests.chaostests.ChaosConductor"><appender-ref ref="HARNESS"/></logger>
    <logger name="bz.stub.parallelconsumer.integrationTests.chaostests.ChaosScenarioBase"><appender-ref ref="HARNESS"/></logger>
    <logger name="bz.stub.parallelconsumer.integrationTests.utils.ManagedPCInstance"><appender-ref ref="HARNESS"/></logger>

    <!-- PC subsystems, split by the question you are asking. -->
    <logger name="bz.stub.parallelconsumer.internal.ConsumerOffsetCommitter"><appender-ref ref="PC_COMMIT"/></logger>
    <logger name="bz.stub.parallelconsumer.internal.ProducerManager"><appender-ref ref="PC_COMMIT"/></logger>
    <logger name="bz.stub.parallelconsumer.offsets"><appender-ref ref="PC_COMMIT"/></logger>

    <logger name="bz.stub.parallelconsumer.internal.BrokerPollSystem"><appender-ref ref="PC_LIFECYCLE"/></logger>
    <logger name="bz.stub.parallelconsumer.internal.ConsumerManager"><appender-ref ref="PC_LIFECYCLE"/></logger>
    <logger name="bz.stub.parallelconsumer.internal.AbstractParallelEoSStreamProcessor"><appender-ref ref="PC_LIFECYCLE"/></logger>

    <logger name="bz.stub.parallelconsumer.state"><appender-ref ref="PC_STATE"/></logger>

    <root level="${pc.log.level:-warn}">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="ERRORS"/>
        <appender-ref ref="WARNINGS"/>
    </root>

    <!-- current -->
    <!--    <logger name="bz.stub.parallelconsumer.integrationTests.state.PartitionStateCommittedOffsetIT" level="debug"/>-->

    <!-- primary -->
    <logger name="bz.stub.parallelconsumer" level="${pc.log.level:-warn}"/>

    <!-- #857 silent-stall investigation: uncomment to trace the "asked for work / got none / but work is in
         the queues" condition and the broker poller pause/resume state under contention. Off by default (these
         are verbose and perturb stall timing). See
         docs/solutions/test-flakiness/pc-silent-stall-under-contention-2026-07-29.md -->
    <!--    <logger name="bz.stub.parallelconsumer.state.ShardManager" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.state.WorkManager" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.internal.BrokerPollSystem" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer" level="trace"/>-->

    <!-- ALWAYS ON despite the warn default: the tests' OWN narration, not the product's. What made the
         default noisy is product logging (one ParallelConsumerOptions toString per construction, the
         control loop); a test that deliberately logs a line is saying something worth reading. Above all
         the chaos suite's run-start banner - "=== CHAOS W1 churn storm: seed=... (replay: ...) ===" - which
         is the only copy of the seed on a PASSING run; buildAutopsy() reprints it on failure, but a pass
         that you later want to replay has nothing else. Integration tests have no resources dir of their
         own, so failsafe reads THIS file. -->
    <!-- Same knob, floor of info rather than warn: a hard "info" here would make
         -Dpc.log.level=debug silently NOT raise the integration tests, which is the case you most
         want it for. -->
    <logger name="bz.stub.parallelconsumer.integrationTests" level="${pc.log.level:-info}"/>

    <logger name="org.apache.kafka" level="warn"/>

    <!-- ALWAYS ON despite the blanket warn above: offset-reset decisions are rare (one line each) and
         contain the exact start position - the single number that solved the committedOffsetRemoved[latest]
         nudge race. See docs/solutions/test-flakiness/latest-reset-nudge-race-committedoffsetremoved-2026-07-30.md -->
    <logger name="org.apache.kafka.clients.consumer.internals.SubscriptionState" level="info"/>

    <!-- Standing harness: kafka-client bootstrap diagnostics. Flip to debug when hunting consumer
         bootstrap / first-poll / group-membership issues - the client narrates its whole state machine;
         instrument by un-silencing, not by patching. NB kafka-clients 3.5+ logs the fetch path under
         AbstractFetch (not only Fetcher). See the solution doc above for the method. -->
    <!--    <logger name="org.apache.kafka.clients.consumer.internals.AbstractCoordinator" level="debug"/>-->
    <!--    <logger name="org.apache.kafka.clients.consumer.internals.ConsumerCoordinator" level="debug"/>-->
    <!--    <logger name="org.apache.kafka.clients.consumer.internals.SubscriptionState" level="debug"/>-->
    <!--    <logger name="org.apache.kafka.clients.consumer.internals.AbstractFetch" level="debug"/>-->
    <!--    <logger name="org.apache.kafka.clients.consumer.internals.Fetcher" level="debug"/>-->
    <!--    <logger name="org.apache.kafka.clients.Metadata" level="debug"/>-->

    <!-- docker logs -->
    <logger name="bz.stub.parallelconsumer.integrationTests.BrokerIntegrationTest" level="warn"/>

    <!--    <logger name="bz.stub.parallelconsumer.internal.utils.LatchTestUtils" level="trace"/>-->

    <!--    <logger name="bz.stub.parallelconsumer.internal.DynamicLoadFactor" level="debug"/>-->

    <!--    <logger name="bz.stub.parallelconsumer" level="debug"/>-->

    <!-- Key classes -->
    <!--        <logger name="bz.stub.parallelconsumer.internal.AbstractParallelEoSStreamProcessor" level="debug"/>-->
    <!-- <logger name="bz.stub.parallelconsumer.internal.AbstractParallelEoSStreamProcessor" level="trace"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.state.PartitionStateManager" level="trace"/> -->
    <!--        <logger name="bz.stub.parallelconsumer.state" level="trace"/>-->

    <!-- Offset committing-->
    <!--    <logger name="bz.stub.parallelconsumer.internal.AbstractOffsetCommitter" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.offsets" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.offsets.OffsetSimultaneousEncoder" level="debug"/>-->

    <!-- Broker polling -->
    <!--    <logger name="bz.stub.parallelconsumer.internal.BrokerPollSystem" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.internal.BrokerPollSystem" level="trace"/>-->
    <!--            <logger name="bz.stub.parallelconsumer.internal" level="debug"/>-->
    <!--            <logger name="bz.stub.parallelconsumer.state.WorkManager" level="debug"/>-->

    <!--    <logger name="bz.stub.parallelconsumer.BatchTestMethods" level="trace"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.state.RemovedPartitionState" level="trace"/>-->

    <!--    <logger name="bz.stub.parallelconsumer.ParallelEoSStreamProcessorPauseResumeTest" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.AbstractParallelEoSStreamProcessorTestBase" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.internal.utils.LongPollingMockConsumer" level="trace"/>-->

    <!--    <logger name="bz.stub.parallelconsumer" level="debug"/>-->
    <!--        <logger name="bz.stub.parallelconsumer" level="trace"/>-->
    <!--    <logger name="bz.stub.parallelconsumer" level="error"/>-->

    <!--            <logger name="bz.stub.parallelconsumer" level="trace" />-->

    <!-- notable specifics -->
    <!--    <logger name="bz.stub.parallelconsumer.offsets.OffsetMapCodecManager" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.offsets.OffsetRunLength" level="trace"/>-->

    <!-- Tests    -->
    <!--    <logger name="bz.stub.parallelconsumer.BatchTestMethods" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.ParallelEoSStreamProcessorTest" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.ParallelEoSStreamProcessorTestBase" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.integrationTests" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.integrationTests.LargeVolumeInMemoryTests" level="debug"/>-->

    <!--    <logger name="bz.stub.parallelconsumer.offsets.WorkManagerOffsetMapCodecManagerTest" level="debug"/>-->

    <!-- containers -->
    <!--    <logger name="bz.stub.parallelconsumer.integrationTests.DbTest" level="debug"/>-->
    <!--    <logger name="bz.stub.parallelconsumer.integrationTests.BrokerIntegrationTest" level="debug"/>-->

    <!-- related -->
    <!--    <logger name="org.apache.kafka.clients" level="info"/>-->
    <!--    <logger name="org.apache.kafka.clients" level="warn"/>-->
    <!--    <logger name="org.apache.kafka.clients" level="debug"/>-->

    <!-- Kafka External -->
    <logger name="org.apache.kafka.common.config.AbstractConfig" level="error"/>

    <!-- external -->
    <logger name="org.postgresql" level="info"/>
    <!--    <logger name="io.vertx" level="debug"/>-->
    <logger name="io.netty" level="info"/>
    <logger name="pl.tlinkowski" level="warn"/>

    <!-- kafka internal -->
    <!--    <logger name="org.apache.kafka.clients.consumer.internals.ConsumerCoordinator" level="debug"/>-->

</configuration>
