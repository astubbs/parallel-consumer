<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright (C) 2026 Antony Stubbs and contributors

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>bz.stub.parallelconsumer</groupId>
        <artifactId>parallel-consumer-parent</artifactId>
        <version>0.6.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>parallel-consumer-streams</artifactId>
    <name>Kafka Parallel Consumer - Kafka Streams (ALPHA - EXPERIMENTAL, NOT PUBLISHED)</name>
    <description>
        ALPHA / EXPERIMENTAL - NOT PRODUCTION READY, AND NOT PUBLISHED.

        Runs a Kafka Streams processor chain on Parallel Consumer's WorkManager and worker pool instead
        of the serial PartitionGroup.nextRecord() -> StreamTask.process() loop, so records on distinct
        keys of one partition execute concurrently through the unmodified ProcessorNode chain. A
        topology's own code does not change.

        THE SEAM IS OFF BY DEFAULT - this is an opt-in preview. Turn it on for a JVM with
        -Dpc.streams.dispatch.enabled=true. Joins, windows, suppression, versioned and session stores
        and EOS are unsupported and are now REFUSED by name, at build time and at task construction,
        rather than allowed to return silently wrong answers. Task lifecycle no longer holds it either:
        a revived task rebuilds its dispatcher, and a task changing partitions in a cooperative
        rebalance carries PC's assignment with it. Nor does exception surfacing: a control-flow
        exception raised inside a processor now keeps its type and reaches Kafka's recovery from the
        pump that dispatched the record, nothing commits past a worker failure, and inflow is bounded
        by the same buffered.records.per.partition stock uses. What the default waits on now is a
        reconciliation step rather than a named defect - the seam-on measurement has to be re-taken on
        the merged module. PcDispatchSwitch's javadoc owns that decision and the measurement behind it.

        Build machinery: the named classes are unpacked from the published kafka-streams sources jar,
        a tracked patch is applied, and the result is compiled into this module's own target/classes,
        which precedes the kafka-streams jar on the classpath. Apache Kafka's own compiled test suite
        is then run against the patched classes, with the seam OFF, as the behaviour-preservation
        oracle. Issue astubbs#255.

        NO APACHE KAFKA SOURCE IS COMMITTED TO THIS REPOSITORY. The patched Kafka classes are generated
        at build time; only src/main/patch/*.patch is tracked. README.md owns the mechanism and the
        developer loop, and docs/solutions/architecture-patterns/patch-a-dependency-at-build-time-without-vendoring-it.md
        owns the technique in general.
    </description>

    <properties>
        <!-- PUBLICATION IS OFF FOR THIS MODULE, DELIBERATELY - "merge freely, publish deliberately".
             Merging an isolated leaf module is cheap to reverse; publishing an artifact that carries
             compiled, modified Apache Kafka classes in Apache's own package namespace is not. The
             split-package hazard that makes publication a decision rather than a formality is stated in
             README.md, "The classpath hazard this module does not solve".

             maven.install.skip is deliberately NOT set: a local `./mvnw install` still puts the jar in
             the local repository, which is how anyone experiments with it. It is the remote publication
             that is off. Ordering matters as well as the flags - see the <module> comment in the root
             pom for the central-publishing-maven-plugin bug this module must not be positioned into. -->
        <maven.deploy.skip>true</maven.deploy.skip>
        <gpg.skip>true</gpg.skip>

        <!-- Where the unpacked-then-patched Kafka sources land. Generated, gitignored, never tracked.

             Deliberately NOT under target/generated-sources: the root pom adds that whole directory as an
             *integration-test* source root for every module (see its build-helper add-integration-test-source
             execution). Generating there compiles these classes a second time into target/test-classes,
             which then precedes target/classes on the surefire classpath - two copies of the same patched
             Kafka classes, and the one that wins is an accident of classpath order rather than a decision. -->
        <kafka.patched.dir>${project.build.directory}/kafka-patched</kafka.patched.dir>
        <kafka.pristine.dir>${project.build.directory}/kafka-pristine</kafka.pristine.dir>

        <!-- The classes this module patches. Named deliberately, not discovered.

             AbstractProcessorContext holds the current record and the current processor node in two
             `protected` fields that stock Streams gets away with because exactly one thread is ever inside
             a task. Thread-confining them is the groundwork every concurrent execution seam needs, and it
             cannot be done in isolation: ProcessorContextImpl reads and writes those inherited fields
             directly, so it has to move to the accessors in the same change or the confinement is bypassed
             by getfield/putfield without a single compile error to say so.

             RecordCollectorImpl is here for a different reason and is the one the compiler would never have
             demanded: it is constructed outside StreamTask, and its non-concurrent maps are written from
             the producer's I/O thread for every in-flight send.

             StreamTask is the execution seam itself - the point where a record is selected and the processor
             chain is run. It is the only class here that references Parallel Consumer, which is what let the
             rung below this one (astubbs#379) carry the three above without it.

             StreamThread owns the poll wait, and nothing else can split it. Wake-on-work needs a worker
             completion to end that wait instead of letting it run out; see PcWorkSignal. It has the widest
             blast radius of the five if a future Kafka reshapes the poll phase, which is why it has its own
             kill switch and why StreamThreadTest joins the upstream oracle below.

             If this list has to grow much past a dozen, that sprawl is itself the answer to "how little had
             to change" - see the "Do not apply when" section of the technique write-up.

             The eight kstream entries are the refusal envelope (astubbs#255), and they come in matched pairs
             for a reason the compiler enforces. A call site resolves to the symbol its receiver's STATIC TYPE
             declares, so `stream.join(...)` against a KStream variable resolves to KStream#join - an
             annotation on KStreamImpl is invisible there. That is why the four public INTERFACES carry
             @DoNotCall/@Deprecated. Conversely an interface method has no body to throw from, which is why
             the four IMPLS carry the runtime refusal. Neither half substitutes for the other.

             This list is now thirteen, which is past the point the sentence above sets rather than at it.
             Stated rather than absorbed: the smaller alternative is a single check in InternalStreamsBuilder
             keyed on the DSL's graph-node types, at the cost of a message one step removed from the method
             the user actually called. It reaches ten, not five - the four IMPLS go away and
             InternalStreamsBuilder arrives, but the four INTERFACES stay patched, because the compile-time
             refusal is the half that has to sit on the symbol a call site resolves to. -->
        <patched.classes>org/apache/kafka/streams/processor/internals/AbstractProcessorContext.java,org/apache/kafka/streams/processor/internals/ProcessorContextImpl.java,org/apache/kafka/streams/processor/internals/RecordCollectorImpl.java,org/apache/kafka/streams/processor/internals/StreamTask.java,org/apache/kafka/streams/processor/internals/StreamThread.java,org/apache/kafka/streams/kstream/KStream.java,org/apache/kafka/streams/kstream/KTable.java,org/apache/kafka/streams/kstream/KGroupedStream.java,org/apache/kafka/streams/kstream/CogroupedKStream.java,org/apache/kafka/streams/kstream/internals/KStreamImpl.java,org/apache/kafka/streams/kstream/internals/KTableImpl.java,org/apache/kafka/streams/kstream/internals/KGroupedStreamImpl.java,org/apache/kafka/streams/kstream/internals/CogroupedKStreamImpl.java</patched.classes>

        <!-- Module-local and explicitly versioned on purpose: pinning jackson-databind in root
             dependencyManagement forces WireMock (parallel-consumer-vertx) onto an incompatible Jackson and
             breaks VertxTest.

             Deliberately AHEAD of the 2.16.2 that kafka-streams 3.9.2 resolves, rather than matching it.
             2.16.2 carries two high-severity PolymorphicTypeValidator bypasses (GHSA-j3rv-43j4-c7qm,
             GHSA-rmj7-2vxq-3g9f), both first patched at 2.18.8, and this declaration is compile scope, so
             the version chosen here is the one that would reach anyone depending on this module. Neither
             advisory is reachable from the generated sources - Kafka's use here is metadata
             serialisation, not polymorphic deserialisation of untrusted input - but carrying a flagged
             version to save a patch bump is not a trade worth making. Held on the 2.18 line to stay close
             to what the jar was built against. -->
        <kafka.streams.jackson.version>2.18.9</kafka.streams.jackson.version>

        <!-- Only used by Kafka's own test suite (see the build section). Matched to what Kafka 3.9 builds
             its own tests against, so their assertions mean what their authors meant. -->
        <hamcrest.version>2.2</hamcrest.version>
        <reload4j.version>1.2.25</reload4j.version>

        <!-- Kafka's own test FIXTURES need the same treatment as its main classes, for the same reason.
             InternalMockProcessorContext extends AbstractProcessorContext and, as shipped, reads and
             writes the `recordContext` / `currentNode` fields directly. Thread-confining those fields
             turns every one of those getfield/putfield instructions into a NoSuchFieldError at
             construction time, so every RecordCollectorTest case dies before reaching a single assertion.
             Regenerating the fixture from the published test-sources jar and converting it to the
             accessors - exactly what the main patch does to ProcessorContextImpl - is what lets those
             tests run at all. Still no Apache source in the repository: generated, gitignored, only the
             patch is tracked. -->
        <kafka.test.patched.dir>${project.build.directory}/kafka-test-patched</kafka.test.patched.dir>
        <kafka.test.pristine.dir>${project.build.directory}/kafka-test-pristine</kafka.test.pristine.dir>
        <patched.test.classes>org/apache/kafka/test/InternalMockProcessorContext.java</patched.test.classes>

        <!-- THE SEAM-ON EVIDENCE LANE, opt-in. See the kafka-upstream-tests-seam-on execution for what it
             measures and bin/ci-streams-seam-on-evidence.sh for the lane that reads it. Off here because the
             seam-on run is EXPECTED to have failures - they are the measurement - and a red gate on every
             build trains people to ignore it. -->
        <seam.on.upstream.skip>true</seam.on.upstream.skip>

        <!-- Whether a failure in the seam-OFF oracle stops the build. TRUE everywhere except inside the
             seam-on evidence lane, and the exception is the point rather than a loosening.

             In an ordinary build the oracle is the behaviour-preservation gate and a failure must be fatal.
             In the lane the same run is the CONTROL ARM, and stopping on its first failure means the
             measured arm never runs at all - so a known, already-diagnosed flake in Kafka's own suite costs
             a fifteen-minute re-run and teaches exactly the habit this repository has a write-up against.
             The lane relaxes this ONE execution and then makes the verdict itself: every control-arm failure
             must carry a flaky-case marker in the inflight ledger, or SeamOnDivergenceLaneTest fails. That
             assertion is not relaxed, so nothing is being waved through - the failure is being classified
             instead of being allowed to abort the measurement. -->
        <seam.off.upstream.failure.ignore>false</seam.off.upstream.failure.ignore>

        <!-- Handed to the classifier so a report directory left behind by an earlier run cannot be read as
             this run's result. Surefire forks a fresh JVM per execution, all of them after this timestamp is
             stamped, so "older than the build started" is exactly the stale case. Format pinned because the
             classifier parses it; module-local, so nothing outside this module sees the change. -->
        <maven.build.timestamp.format>yyyy-MM-dd'T'HH:mm:ss'Z'</maven.build.timestamp.format>
    </properties>

    <dependencies>
        <!-- The seam's own dependency, and the reason this module stopped being pure build machinery: the
             generated StreamTask calls into PcTaskDispatcher, which drives core's WorkManager directly. -->
        <dependency>
            <groupId>bz.stub.parallelconsumer</groupId>
            <artifactId>parallel-consumer-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-streams</artifactId>
            <version>${kafka.version}</version>
        </dependency>

        <!-- jackson-databind is runtime-scope in the kafka-streams POM, so the generated sources will
             not compile without it promoted to compile scope here. (slf4j-api has the same problem but
             needs no declaration - the root pom's <dependencies> already gives it to every module.) -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${kafka.streams.jackson.version}</version>
        </dependency>
        <!-- Not optional despite nothing here importing it directly. kafka-streams pulls
             jackson-annotations 2.16.2 transitively while the databind above pulls 2.18.9, and the root
             pom's enforcer RequireUpperBoundDeps rule fails the build on that pairing - correctly, since
             the resolved 2.16.2 would be older than what databind was built against. Declaring it here at
             the same version is the fix that keeps both halves of Jackson on one line. -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <version>${kafka.streams.jackson.version}</version>
        </dependency>

        <!-- Tests -->

        <!-- The shared TestConventionRules that TestConventionsArchTest points ArchUnit at, and
             BrokerIntegrationTest, which is what BrokerStreamsIntegrationTest extends to reach a
             TestContainers broker. Core's MAIN classes come from the compile-scope declaration above, which
             the test jar also needs at runtime: a test-scoped dependency's own dependencies do not come
             with it, and the core tests jar registers MyRunListener as a JUnit TestExecutionListener
             through ServiceLoader, which reaches into core's StringUtils. -->
        <dependency>
            <groupId>bz.stub.parallelconsumer</groupId>
            <artifactId>parallel-consumer-core</artifactId>
            <version>${project.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- TopologyTestDriver, which the refusal envelope's Processor API backstop arms build a real
             topology against. It runs the topology on the calling thread, which is exactly right here and
             exactly wrong for the dispatch arms below: a refusal is thrown while the task is CONSTRUCTED, so
             no worker pool and no broker are needed to observe it, and the driver is several orders of
             magnitude cheaper than a container per case. -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-streams-test-utils</artifactId>
            <version>${kafka.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- The broker-backed arms: BrokerIntegrationTest starts a TestContainers Kafka, and the seam-on
             proofs need a real StreamThread against a real broker - a TopologyTestDriver runs the topology
             on the calling thread and so cannot exercise either the worker pool or the poll wait. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>kafka</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- BrokerIntegrationTest (core test-jar) uses commons-lang3, and test-scope dependencies are not
             transitive - without this the container fails to start with NoClassDefFoundError. -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Apache Kafka's OWN test suite, and the fixtures it is built on. These are what make the
             upstream execution below possible; see the comment on that surefire execution. -->

        <!-- Kafka's compiled tests, plus the org.apache.kafka.test.* fixtures they are built on
             (InternalMockProcessorContext, MockSourceNode, MockClientSupplier, ...). Preferred over
             recompiling test-sources: no Apache source enters the build, and no Java 8 problem. -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-streams</artifactId>
            <version>${kafka.version}</version>
            <classifier>test</classifier>
            <scope>test</scope>
        </dependency>
        <!-- org.apache.kafka.test.TestUtils, org.apache.kafka.common.utils.MockTime and
             LogCaptureAppender live here, not in the streams test jar. -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
            <version>${kafka.version}</version>
            <classifier>test</classifier>
            <scope>test</scope>
        </dependency>
        <!-- LogCaptureAppender is a log4j 1.x Appender; RecordCollectorTest imports
             org.apache.log4j.Level directly. reload4j is the maintained drop-in for that API. -->
        <dependency>
            <groupId>ch.qos.reload4j</groupId>
            <artifactId>reload4j</artifactId>
            <version>${reload4j.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Having the log4j 1.x API on the classpath is not enough: LogCaptureAppender only sees a
             message if SLF4J actually routes to log4j. Under logback it captures nothing and
             RecordCollectorTest's one log-assertion test fails on an empty list. This binding is
             swapped in for the Kafka execution only - see classpathDependencyExcludes below. -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-reload4j</artifactId>
            <version>${slf4j.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.hamcrest</groupId>
            <artifactId>hamcrest</artifactId>
            <version>${hamcrest.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- 1. Unpack the target classes from the published sources jar, twice: a pristine copy
                    (used by bin/regen-patch.sh to re-derive the patch) and the working copy that gets
                    patched. Always overwrite, so every build starts from released source and the patch
                    applies to a clean tree rather than to itself.

                    The second execution does the same for Kafka's own test FIXTURE - see the
                    kafka.test.patched.dir property for why the fixture needs patching too. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-kafka-streams-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <overWriteReleases>true</overWriteReleases>
                            <overWriteSnapshots>true</overWriteSnapshots>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.apache.kafka</groupId>
                                    <artifactId>kafka-streams</artifactId>
                                    <version>${kafka.version}</version>
                                    <classifier>sources</classifier>
                                    <type>jar</type>
                                    <includes>${patched.classes}</includes>
                                    <outputDirectory>${kafka.patched.dir}</outputDirectory>
                                </artifactItem>
                                <artifactItem>
                                    <groupId>org.apache.kafka</groupId>
                                    <artifactId>kafka-streams</artifactId>
                                    <version>${kafka.version}</version>
                                    <classifier>sources</classifier>
                                    <type>jar</type>
                                    <includes>${patched.classes}</includes>
                                    <outputDirectory>${kafka.pristine.dir}</outputDirectory>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>unpack-kafka-streams-test-sources</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <overWriteReleases>true</overWriteReleases>
                            <overWriteSnapshots>true</overWriteSnapshots>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.apache.kafka</groupId>
                                    <artifactId>kafka-streams</artifactId>
                                    <version>${kafka.version}</version>
                                    <classifier>test-sources</classifier>
                                    <type>jar</type>
                                    <includes>${patched.test.classes}</includes>
                                    <outputDirectory>${kafka.test.patched.dir}</outputDirectory>
                                </artifactItem>
                                <artifactItem>
                                    <groupId>org.apache.kafka</groupId>
                                    <artifactId>kafka-streams</artifactId>
                                    <version>${kafka.version}</version>
                                    <classifier>test-sources</classifier>
                                    <type>jar</type>
                                    <includes>${patched.test.classes}</includes>
                                    <outputDirectory>${kafka.test.pristine.dir}</outputDirectory>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- 2. Apply the tracked patches. The script dry-runs first and fails the build on any
                    rejected hunk - a half-applied patch is the worst failure available here. An empty
                    patch is an explicit no-op, which is the control arm that falsifies the harness
                    independently of anything the patch intends to change.

                    Bound to process-sources / process-test-sources, NOT the matching generate-* phase, so
                    each is ordered AFTER the unpack above by lifecycle phase. Within a single phase Maven
                    orders executions by plugin declaration order in the EFFECTIVE pom - and the root pom
                    declares exec-maven-plugin before maven-dependency-plugin, so a same-phase binding runs
                    the patch before the sources it patches exist. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>apply-kafka-patch</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>${project.basedir}/bin/apply-patch.sh</executable>
                            <arguments>
                                <argument>${kafka.patched.dir}</argument>
                                <argument>${project.basedir}/src/main/patch/pc-streams.patch</argument>
                            </arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>apply-kafka-test-patch</id>
                        <phase>process-test-sources</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>${project.basedir}/bin/apply-patch.sh</executable>
                            <arguments>
                                <argument>${kafka.test.patched.dir}</argument>
                                <argument>${project.basedir}/src/main/patch/pc-streams-testfixtures.patch</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- 3. Compile the patched sources into target/classes, which precedes the kafka-streams
                    jar on the classpath - so these win and their siblings still load from the jar.
                    The patched fixture goes in as a TEST source root, so it lands in target/test-classes,
                    which precedes the kafka-streams TEST jar for exactly the same reason. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>add-patched-kafka-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${kafka.patched.dir}</source>
                            </sources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>add-patched-kafka-test-sources</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>add-test-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${kafka.test.patched.dir}</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- 4. THE ORACLE. Apache Kafka's OWN test suite, run against our patched classes, in the
                    module's NORMAL test run - no profile, no flag, every build.

                    This is the strongest behaviour-preservation gate available: Kafka publishes its
                    compiled tests to Central, our patched classes precede the kafka-streams jar on the
                    classpath (proven directly by ShadowedClassLoadingTest), so Kafka's own
                    ProcessorContextImplTest and RecordCollectorTest exercise OUR AbstractProcessorContext,
                    OUR ProcessorContextImpl and OUR RecordCollectorImpl - not the released ones. Anything
                    the patch broke that Kafka tested fails here.

                    StreamTaskTest is included although StreamTask itself is NOT in patched.classes: it is
                    the richest exercise of the patched context and collector there is, driving both
                    through a real task rather than a fixture. It is also where the seam lands next, so
                    keeping it in the include list now means the seam's arrival changes a number rather
                    than introducing a suite.

                    THE COUNTS ARE A CITABLE CLAIM AND MUST BE RE-DERIVED, NEVER COPIED. They move with
                    the patch, with the Kafka version and with the seam. Read them out of
                    target/surefire-reports-kafka-upstream/ after running the module's whole `test` phase.
                    Do NOT scope the run with -Dtest=: it silently overrides the <includes> below, this
                    suite does not run at all, and the build still goes green having computed nothing.

                    Surefire only scans target/test-classes by default, so <dependenciesToScan> is what
                    makes it look inside the kafka-streams test jar at all. It sits on this extra
                    execution and NOT on the plugin, so the default-test execution (our own tests) is
                    unaffected. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <!-- Exactly one SLF4J binding per fork. Our own tests keep logback (and
                     logback-test.xml); Kafka's fork gets reload4j so LogCaptureAppender works.
                     Both bindings are on the module's test classpath - surefire is what keeps
                     them apart, per execution. -->
                <configuration>
                    <classpathDependencyExcludes>
                        <classpathDependencyExclude>org.slf4j:slf4j-reload4j</classpathDependencyExclude>
                    </classpathDependencyExcludes>
                    <!-- Handed to ShadowedClassLoadingTest so it can assert its own list against the real
                         property rather than against a "keep in sync" comment. A class added here and not
                         there would otherwise be silently unproven - which is precisely the false positive
                         that test exists to prevent. -->
                    <systemPropertyVariables>
                        <patched.classes>${patched.classes}</patched.classes>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <id>kafka-upstream-tests</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <!-- False in every ordinary build: this execution IS the behaviour-preservation
                                 gate. The seam-on evidence lane sets it true, because there this run is the
                                 control arm and aborting on its first failure means the measured arm never
                                 runs - see the property's own comment. -->
                            <testFailureIgnore>${seam.off.upstream.failure.ignore}</testFailureIgnore>
                            <dependenciesToScan>
                                <dependency>org.apache.kafka:kafka-streams</dependency>
                            </dependenciesToScan>
                            <!-- Only the tests of the classes we patch. Scanning the whole jar would run
                                 ~2500 tests of code this module never touches, and drown the signal.

                                 StreamThreadTest joined this list with the fifth patched class. The rule is
                                 "the tests of the classes we patch", so patching StreamThread without it
                                 would have left the widest-blast-radius class as the one part of the patch
                                 with no upstream suite behind it. -->
                            <includes>
                                <include>org/apache/kafka/streams/processor/internals/StreamTaskTest.java</include>
                                <include>org/apache/kafka/streams/processor/internals/StreamThreadTest.java</include>
                                <include>org/apache/kafka/streams/processor/internals/ProcessorContextImplTest.java</include>
                                <include>org/apache/kafka/streams/processor/internals/RecordCollectorTest.java</include>
                            </includes>
                            <reportsDirectory>${project.build.directory}/surefire-reports-kafka-upstream</reportsDirectory>
                            <!-- The oracle's independence from the seam. Written by the rung below this one
                                 (astubbs#379) ahead of any reader; PcDispatchSwitch is now that reader, and
                                 the pin stays exactly as it was.

                                 It is deliberately NOT redundant with the seam defaulting off. This
                                 execution's claim - Kafka's own suite passes against the patched classes -
                                 only means anything with the seam OFF, and it is a claim about the PATCH,
                                 not about a default. Deleting the pin because the default agrees with it
                                 today would make the oracle silently follow the default the day it moves. -->
                            <systemPropertyVariables>
                                <pc.streams.dispatch.enabled>false</pc.streams.dispatch.enabled>
                            </systemPropertyVariables>
                            <classpathDependencyExcludes>
                                <classpathDependencyExclude>ch.qos.logback:logback-classic</classpathDependencyExclude>
                            </classpathDependencyExcludes>
                            <!-- Kafka's tests are written for a serial JUnit runner: StreamTaskTest
                                 pins a fixed state-directory name and MockProducer/MockConsumer are
                                 shared per instance, so running them concurrently fails most of them
                                 on state-dir locks alone and says nothing about our patch.
                                 Concurrency arrives on this module by accident anyway - the
                                 parallel-consumer-core tests jar ships a junit-platform.properties
                                 with parallel.enabled=true, and it lands on our test classpath.
                                 Surefire's configurationParameters outrank that file. -->
                            <properties>
                                <configurationParameters>
                                    junit.jupiter.execution.parallel.enabled = false
                                </configurationParameters>
                            </properties>
                            <!-- Kafka's tests are not written for our tag scheme; the inherited
                                 group filters would silently drop all of them. -->
                            <groups combine.self="override"/>
                            <excludedGroups combine.self="override"/>
                        </configuration>
                    </execution>

                    <!-- 4b. THE SAME UPSTREAM CLASSES, SEAM ON. The execution above is a
                         BEHAVIOUR-PRESERVATION claim - the patch changes nothing while the switch is off.
                         This one is the opposite measurement: what Kafka's own suite says about the path this
                         module actually ships. It is the evidence the refusal envelope's reinstatement rule
                         needs, because a refused construct only comes back when Kafka's suite exercises it
                         seam-ON and passes.

                         SKIPPED BY DEFAULT and testFailureIgnore=true, because the failures ARE the
                         measurement rather than a regression. What turns that measurement into a gate is the
                         execution after it: every failure seam-on that was green seam-off must be explained
                         by a named mechanism, and one that is not fails the lane. So this execution never
                         goes red and the classifier does - which is the only arrangement in which "the
                         failures are expected" and "a new divergence fails the build" are both true.

                         Run it through bin/ci-streams-seam-on-evidence.sh, which deletes both report
                         directories first. Do NOT use -Dtest= to narrow it: that overrides the includes
                         below, the suite never runs, and the build goes green having computed nothing. -->
                    <execution>
                        <id>kafka-upstream-tests-seam-on</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <skipTests>${seam.on.upstream.skip}</skipTests>
                            <testFailureIgnore>true</testFailureIgnore>
                            <dependenciesToScan>
                                <dependency>org.apache.kafka:kafka-streams</dependency>
                            </dependenciesToScan>
                            <!-- Deliberately the SAME include list as the seam-off execution above. The
                                 classifier set-differences the two arms case by case, and a case present in
                                 one arm and absent from the other cannot be differenced - it would be
                                 silently dropped from the divergence set. SeamOnDivergenceClassifier asserts
                                 the two arms cover the same classes rather than trusting this comment. -->
                            <includes>
                                <include>org/apache/kafka/streams/processor/internals/StreamTaskTest.java</include>
                                <include>org/apache/kafka/streams/processor/internals/StreamThreadTest.java</include>
                                <include>org/apache/kafka/streams/processor/internals/ProcessorContextImplTest.java</include>
                                <include>org/apache/kafka/streams/processor/internals/RecordCollectorTest.java</include>
                            </includes>
                            <reportsDirectory>${project.build.directory}/surefire-reports-kafka-upstream-seam-on</reportsDirectory>
                            <systemPropertyVariables>
                                <pc.streams.dispatch.enabled>true</pc.streams.dispatch.enabled>
                            </systemPropertyVariables>
                            <classpathDependencyExcludes>
                                <classpathDependencyExclude>ch.qos.logback:logback-classic</classpathDependencyExclude>
                            </classpathDependencyExcludes>
                            <!-- Same serial-runner requirement as the seam-off execution above. -->
                            <properties>
                                <configurationParameters>
                                    junit.jupiter.execution.parallel.enabled = false
                                </configurationParameters>
                            </properties>
                            <groups combine.self="override"/>
                            <excludedGroups combine.self="override"/>
                        </configuration>
                    </execution>

                    <!-- 4c. THE GATE ON 4b. Reads both report directories, set-differences them, and
                         classifies every divergence by the mechanism its own recorded failure names.
                         Explained divergences pass; one that matches no known mechanism fails this
                         execution, and therefore the build.

                         Runs in the same opt-in as 4b, and LAST, because it reads what 4b wrote. It is
                         excluded from the default test execution by name for the same reason - run before
                         the reports exist it can only report their absence. -->
                    <execution>
                        <id>seam-on-evidence-classification</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <skipTests>${seam.on.upstream.skip}</skipTests>
                            <includes>
                                <include>**/SeamOnDivergenceLaneTest.java</include>
                            </includes>
                            <reportsDirectory>${project.build.directory}/surefire-reports-seam-on-lane</reportsDirectory>
                            <systemPropertyVariables>
                                <seam.on.reports.dir>${project.build.directory}/surefire-reports-kafka-upstream-seam-on</seam.on.reports.dir>
                                <seam.off.reports.dir>${project.build.directory}/surefire-reports-kafka-upstream</seam.off.reports.dir>
                                <seam.on.evidence.buildStartedAt>${maven.build.timestamp}</seam.on.evidence.buildStartedAt>
                                <patched.classes>${patched.classes}</patched.classes>
                            </systemPropertyVariables>
                            <groups combine.self="override"/>
                            <excludedGroups combine.self="override"/>
                        </configuration>
                    </execution>

                    <!-- The classifier is not part of the module's ordinary test run: it reads report
                         directories that only exist once 4b has run, so here it could only ever report their
                         absence. Named rather than tagged, because the two Kafka executions above already
                         override the group filters and a tag would not reach them.

                         THE OTHER TWO ENTRIES ARE THE ROOT POM'S AND ARE REPEATED ON PURPOSE. An <excludes>
                         list here REPLACES the inherited one rather than adding to it, so declaring only the
                         classifier silently pulled every `integrationTests` class - which is where this
                         module's broker-backed arms live - into the surefire `test` phase, making the unit
                         lane need Docker. It went unnoticed for exactly one build, because everything passed.
                         combine.children="append" would express the intent, but a literal list is what a
                         reader can check against the root pom without knowing that attribute exists. -->
                    <execution>
                        <id>default-test</id>
                        <configuration>
                            <excludes>
                                <exclude>**/integrationTest*/**/*.java</exclude>
                                <exclude>**/*IT.java</exclude>
                                <exclude>**/SeamOnDivergenceLaneTest.java</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- 5. The same SLF4J-binding split as surefire above, on the OTHER lane - and it was missing.
                    That exclusion sits on the surefire plugin's configuration, which failsafe does not
                    inherit, so this module's own INTEGRATION tests were binding reload4j while the pom's
                    comment above said "our own tests keep logback (and logback-test.xml)". Nothing went red
                    for it, because nothing asserted on a log line in that lane until
                    PunctuatorWarningTest did - and what it got was a ClassCastException casting an
                    org.slf4j.reload4j adapter to a logback Logger.

                    The Kafka-fork execution is surefire's and keeps reload4j, so LogCaptureAppender still
                    works there; this only makes the failsafe lane agree with the unit lane and with
                    src/test/resources/logback-test.xml, which was already being ignored here. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <classpathDependencyExcludes>
                        <classpathDependencyExclude>org.slf4j:slf4j-reload4j</classpathDependencyExclude>
                    </classpathDependencyExcludes>
                </configuration>
            </plugin>

            <!-- The generated Apache Kafka classes are not ours to hold to this project's static-analysis
                 rules. `target/classes` here holds nothing else, so this exclusion is the whole module -
                 and it says something true rather than hiding something of ours: the root pom's
                 jdk-unsafe signatures fire on locale-dependent String.format calls that are in Kafka's
                 RELEASED source, at sites the patch does not touch. Fixing them would mean patching
                 Apache code for a rule Apache never adopted, growing the change-set that this module's
                 whole design exists to keep small and re-derivable.

                 Everything of ours in this module - PcTaskDispatcher, PcWorkSignal and the rest of the
                 seam - sits under bz.stub and is NOT matched by this pattern, so it gets the same
                 analysis as every other module. The same scoping decision, for the same reason, is
                 already made for ArchUnit in TestConventionsArchTest. -->
            <plugin>
                <groupId>de.thetaphi</groupId>
                <artifactId>forbiddenapis</artifactId>
                <configuration>
                    <excludes>
                        <exclude>org/apache/kafka/**</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- Publication off, the plugin's own switch. The properties above stop deploy and signing;
                 this stops the Central bundle from carrying the module. See the properties block for why
                 publication is a deliberate decision rather than a default. -->
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <configuration>
                    <skipPublishing>true</skipPublishing>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
